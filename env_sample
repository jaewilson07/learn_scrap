# This is a sample environment file.
# Copy this file to .env and replace the placeholder values with your actual values.
STARLET_SECRET_KEY=your-secret-key
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
NGROK_AUTHTOKEN=your-ngrok-authtoken
PUBLIC_DOMAIN=your-public-domain
UVICORN_PORT=8001
ENV=local

# Supabase/Postgres (behind API)
# For docker-compose using the included db service:
# DATABASE_URL=postgresql://app:app@db:5432/app
DATABASE_URL=postgresql://app:app@localhost:5432/app

# JWT used to mint access tokens for the extension
API_JWT_SECRET=change-me-to-a-long-random-secret
API_JWT_ISSUER=legendary_potato
API_JWT_TTL_SECONDS=604800

# CORS: allow extension origin(s) to call the API
# Example: chrome-extension://<your-extension-id>
CORS_ALLOW_ORIGIN_REGEX=chrome-extension://.*

# In production, restrict which extension return_to URLs can receive tokens.
# Comma-separated list. Entries can be:
# - Full URL: chrome-extension://<id>/auth.html
# - Origin prefix (must end with '/'): chrome-extension://<id>/
EXTENSION_RETURN_TO_ALLOWLIST=
